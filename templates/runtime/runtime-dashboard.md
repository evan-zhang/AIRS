# Runtime Dashboard

## Runtime Plan

- Runtime ID：`{{runtime_id}}`
- Workflow ID：`{{workflow_id}}`
- Status：`{{status}}`

## Agent Graph

记录 Agent 节点、依赖边和并行分支。

## Execution Timeline

按 Event Bus 时间顺序展示任务派发、运行、暂停、恢复、完成、失败和人工等待。

## Event Log

保留 event_id、event_type、timestamp、task_id、agent_id、payload 和 trace_id。

## Context Snapshot

展示输入、引用路径、Memory 摘要、输出和免责声明状态。

## Final State

汇总 task_states、agent_states、quality_gate 和缺口。

免责声明：本文档和示例仅用于 AIRS 工程开发、投资研究流程编排与质量控制，不构成投资建议，不提供荐股、自动交易、交易指令、目标价或收益承诺。
